# deformable-object-manipulation

### Installation:
- `conda env create -f environment.yml` (Python 3.9)
- Install torch for your cuda version https://pytorch.org/get-started/previous-versions/
- Finally, from the main repo directory `pip install .`
